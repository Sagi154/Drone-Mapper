# Drone capabilities
# Drone is modelled as a sphere; radius replaces the old width/length/height.
# 15 cm radius (30 cm diameter) matches the previous horizontal footprint.
min_passable_radius = 15
max_rotate = 90
max_advance = 100
max_elevate = 50

# Lidar
lidar_z_min = 20
lidar_z_max = 120
lidar_d = 2.5
lidar_fov_circles = 5
